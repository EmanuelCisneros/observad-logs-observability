

# URL of the observad bakend — no trailing slash.
NEXT_PUBLIC_API_BASE=http://localhost:8080

# Must match OBSERVA_INGEST_API_KEY on the backend (required for search/stats/tail).
NEXT_PUBLIC_API_KEY=dev-local-key
